# Minecraft 服务端插件 AI 全自动开发 Skill

`develop-minecraft-server-plugin` 是一套面向 Minecraft Java 版服务端插件开发的 Codex Skill。它将通用 AI 编程能力约束为一套可规划、可拆分、可验证、可持续维护的插件工程工作流，适用于从零创建插件、扩展现有项目、修复问题、多版本迁移以及构建交付。

## 核心定位

本 Skill 不以“生成一批能够编译的 Java 文件”为完成标准，而是要求 AI 完成需求结构化、技术选型、开发计划、代码实现、配置设计、自动测试、服务端运行验证和交付说明。

所有开发任务必须先形成计划。复杂插件会按照模块、平台或验证职责拆分为多个子任务，由主任务统一管理公共接口、文件所有权、集成测试和最终交付。

## 版本与平台

- 将 Minecraft 26.2 视为当前版本事实，不再错误地将 1.21 描述为最新版本。
- 默认以 Paper 1.21 作为插件兼容性开发基线。
- 可扩展支持 1.21.6+ 的现代 UI 方向，包括 Dialog 能力。
- 可选支持 Folia，并使用实体、区域、全局和异步调度器的正确线程模型。
- 可选支持 1.12.2 RPG 生态，通过专用平台代码处理旧材料、物品数据、事件和规则差异。
- 当现代版与 1.12.2 差异过大时，优先构建独立 JAR，避免不可靠的全局反射兼容层。

## 插件生态集成

- TabooLib 是显式可选架构，不会被无条件引入。
- 属性功能默认优先兼容 AttributePlus。
- 普通单货币经济优先使用 Vault。
- 多货币经济优先使用 VaultUnlock。
- 点券、充值积分或高级余额优先兼容 PlayerPoints。
- 经验值、等级、饥饿度等原版内容使用独立的原版数值适配器。
- 权限组、继承和临时权限操作优先使用 LuckPerms API。
- 外部变量解析和插件变量输出优先使用 PlaceholderAPI。

所有第三方依赖都通过内部服务边界接入，避免业务逻辑直接绑定特定插件对象，并明确硬依赖、软依赖及缺失时的降级策略。

## GUI 与配置

插件配置优先使用 YAML，并采用 UTF-8 编码。配置项应直接使用含义明确的中文，或使用 YAML 标准的 `#` 添加中文注释。默认配置必须可以直接启动，并包含范围、单位、默认值、版本限制和重载行为等必要说明。

Inventory GUI 使用字符矩阵、语义槽位、显示条件、点击动作、变量和版本化材料构成可验证的配置模型。对于 1.21.6+ 的 Dialog UI，Skill 会复用条件、动作和业务服务，同时将具体渲染实现与传统背包界面隔离。

## 离线 API 资源

Skill 内置可检索的 API 源码快照，覆盖：

- Paper 26.2
- Paper 1.21.1
- Paper 1.21.6
- Spigot/Bukkit 1.12.2

这些资源包含 Paper/Bukkit 类、事件、调度器、物品元数据、Dialog API 和各版本 `Material` 定义。AI 会优先查询本地缓存，无需在每次开发时联网查找基础 API 或重新确认 Minecraft 26.2 是否存在。

## 质量保障

Skill 根据修改风险执行配置校验、编译、静态检查、单元测试、MockBukkit 测试以及 Paper/Folia 测试服验证。每个声称支持的平台都必须独立验证，编译成功不能替代服务端运行测试。

最终交付会说明插件制品、适用版本、Java 要求、依赖、配置迁移、已执行测试、验证结果和剩余限制。

## 使用方式

在 Codex 中调用：

```text
使用 $develop-minecraft-server-plugin 开发一个支持 Paper 1.21 和 Folia 的装备分解插件，GUI 可配置，并兼容 PlaceholderAPI 与 Vault。
```

Skill 会先检查项目并提交开发计划，再根据复杂度决定是否拆分子任务。日常执行通过索引按需加载规范，不会在每次任务中读取完整 Skill。
